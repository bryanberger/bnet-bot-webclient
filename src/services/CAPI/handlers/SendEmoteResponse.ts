import CAPIClient from '../'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const SendEmoteResponse = (client: CAPIClient) =>
  console.log('SendEmoteResponse')

export default SendEmoteResponse
